> Write a function declaration named `repeat` that accepts a string named `message` and a number named `count`

> Write an arrow function with a parameter of `request` with a type of `HTTPRequest`. Annotate the return type as `void`.

> Write a function declaration named `addPerson` that accepts an object named `person` that has `firstName` and `lastName` properties

> Write an arrow function that accepts an object named `person` that has `firstName` and `lastName` properties, but destructures the `lastName` property out

> Write a function declaration named `wrapBoolean` that accepts a value called `value` with a type that's not known ahead of time. Annotate the return type as boolean.

